# Core
openai>=1.35.7
chromadb>=0.5.0
streamlit>=1.32.0
python-dotenv>=1.0.1

# Optional (if you want speech/image extras to work reliably)
# Audio
pydub>=0.25.1          # audio utils (optional, but handy)
soundfile>=0.12.1      # for handling WAV/OGG if needed

# Image
Pillow>=10.2.0         # for optional image post-processing (not strictly required)

# Dev / Editor
ipython>=8.22.0        # optional, for REPL
